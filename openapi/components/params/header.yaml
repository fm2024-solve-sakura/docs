auth:
  in: header
  description: Google Access Token
  name: Authorization
  required: true
  schema:
    type: string
    example: Bearer <token>

auth-optional:
  in: header
  description: Google Access Token
  name: Authorization
  required: false
  schema:
    type: string
    example: Bearer <token>
