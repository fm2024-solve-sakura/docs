auth:
  in: header
  description: GoogleOAuthのAccessToken
  name: Authorization
  required: true
  schema:
    type: string
    example: Bearer <token>

auth-optional:
  in: header
  description: GoogleOAuthのAccessToken
  name: Authorization
  required: false
  schema:
    type: string
    example: Bearer <token>
